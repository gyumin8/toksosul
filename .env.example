# 복사해서 .env 로 이름 바꾸고 값을 채우세요.
# 이 파일은 .gitignore에 들어 있어 깃에 올라가지 않습니다. 키는 반드시 여기에만 둡니다.

# ===== 텍스트: Google Gemini =====
# 키 발급: https://aistudio.google.com/apikey  (AIza... 로 시작)
# 비워두면 목(mock) 응답 모드 -> 키 없이도 전체 플로우 테스트 가능
GEMINI_API_KEY=

# 모델명은 자주 바뀝니다. 404가 나면 아래 값을 바꿔보세요.
# gemini-3.5-flash-lite (싸고 빠름) / gemini-3.5-flash / gemini-2.5-flash
GEMINI_TEXT_MODEL=gemini-3.5-flash-lite

# ===== 이미지: Cloudflare Workers AI (무료) =====
# Account ID: Cloudflare 대시보드 우측의 32자리 문자열
# API Token: 내 프로필 > API Tokens > Create Token > Workers AI 권한 부여
CF_ACCOUNT_ID=
CF_API_TOKEN=
CF_IMAGE_MODEL=@cf/black-forest-labs/flux-1-schnell

# ===== 이미지 저장소: Supabase Storage (무료, 카드 불필요, 배포 환경에서만 필요) =====
# 로컬 개발 중엔 비워두면 static/media에 그냥 저장합니다 (기존과 동일).
# Render 같은 무료 호스팅은 재배포/재시작마다 로컬 디스크가 초기화되므로,
# 배포할 때는 아래 값을 전부 채워서 Supabase Storage에 올리도록 바꿔야 이미지가
# 안 사라집니다. (처음엔 Cloudflare R2를 검토했으나 카드 등록이 필요해 제외했다.)
# Supabase 대시보드 > Storage에서 버킷 생성(Public으로) > Settings의
# "S3 Connection"에서 Access Key ID/Secret 발급.
SUPABASE_PROJECT_REF=
SUPABASE_S3_ACCESS_KEY_ID=
SUPABASE_S3_SECRET_ACCESS_KEY=
SUPABASE_BUCKET=
# 프로젝트 리전 (대시보드 프로젝트 설정에서 확인)
SUPABASE_S3_REGION=us-east-1

# 이미지 벤더 강제 지정: cloudflare / gemini / off
# 비워두면 위 키를 보고 알아서 고릅니다 (Cloudflare 우선)
IMAGE_PROVIDER=

# 삽화 생성을 끄고 플레이스홀더만 쓰려면 0
ENABLE_IMAGE_GEN=1

# ===== 무료 할당량 보호 =====
# 데모 당일 할당량 소진으로 전체가 막히는 걸 막기 위한 벤더별 일일 호출 한도.
# 0(기본값)이면 한도 없음 -> /api/quota로 사용량은 보이지만 막지는 않음.
# 데모 전날, 각 벤더 대시보드에서 그날의 실제 무료 한도를 확인하고
# 안전 마진(80~90%)을 둔 값으로 채워 넣으세요.
#   Gemini 텍스트/이미지: https://aistudio.google.com/usage
#   Cloudflare 이미지: 대시보드 > Workers AI > Usage
GEMINI_TEXT_DAILY_LIMIT=0
GEMINI_IMAGE_DAILY_LIMIT=0
CF_IMAGE_DAILY_LIMIT=0

# ===== 기타 =====
# 개발은 SQLite, 배포 시 PostgreSQL 주소로 교체 (예: Neon — 카드 불필요, 영구 무료)
# Neon 대시보드에서 그대로 복사한 연결 문자열을 넣으면 됩니다 (postgresql://...)
DATABASE_URL=sqlite:///./toksosul.db

# 혼자 테스트할 때만 1. 제출 전에는 이 줄을 지우세요 (서비스 규칙은 2명)
MIN_MEMBERS=1
